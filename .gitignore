# GSD planning docs (local-only per project config)
.planning/
.tmp-execution/

# Node / package managers
node_modules/
.pnpm-store/
.yarn/
.npm/

# Build outputs
dist/
build/
.next/
out/
.expo/
.expo-shared/
web-build/

# Env / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.vs/

# Coverage / test
coverage/
.nyc_output/

# Native (mobile)
ios/Pods/
android/.gradle/
android/app/build/
android/build/

# Supabase
.supabase/
supabase/.branches/
supabase/.temp/
supabase/seed-cache/

# Turborepo
.turbo/

# TypeScript incremental build cache
*.tsbuildinfo

# packages/shared-types/src/database.types.ts is intentionally NOT gitignored (B-01 fix, Plan 03).
# The file is committed to git and tracked alongside migrations.
# (This project is plain JavaScript — the file is a JS stub, not TS codegen output.)
.next-build/
